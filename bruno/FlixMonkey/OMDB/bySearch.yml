info:
    name: bySearch
    type: http
    seq: 3

http:
    method: GET
    url: https://www.omdbapi.com/?apikey={{OMDB_API_KEY}}&s={{title}}
    params:
        - name: apikey
          value: '{{OMDB_API_KEY}}'
          type: query
        - name: s
          value: '{{title}}'
          type: query
    auth: inherit

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5
